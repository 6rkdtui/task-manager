# Task Manager

Данный проект представляет собой программу по управлению задачами. Учебный проект для закрепления знаний Python, ООП и архитектуры приложений на практике.

## Функционал

- создание задачи;
- получение задачи по ID;
- просмотр всех задач;
- изменение задачи;
- отметка задачи выполненной;
- удаление задачи;
- консольное меню;
- обработка некорректного числового ввода;
- хранение задач в базе данных SQLite.

## Используемые технологии

Python, ООП, dataclasses, type hints, SQLite.

## Структура проекта

```text
Task_manager/
├── main.py
├── task_repository.py
├── task_service.py
├── task.py
├── README.md
└── .gitignore
```

## Архитектура

Проект разделён на несколько слоёв:

```text
main.py
↓
TaskService
↓
TaskRepository
↓
SQLite
```

- `main.py` — консольный интерфейс;
- `TaskService` — бизнес-логика приложения;
- `TaskRepository` — работа с базой данных;
- `Task` — модель задачи.

## Работа с базой данных

В версии 0.5 улучшена работа с SQLite:

- курсоры закрываются через `try/finally`;

- при ошибках операций записи выполняется `rollback()`;

- ошибки SQLite после отката пробрасываются выше;

- изменения объектов `Task` выполняются только после успешного `commit()`;

- соединение с базой данных закрывается через `TaskService`.

## Инструкция по запуску

```bash
python main.py
```

## Планы развития

1. ~~Добавить хранение данных в SQLite~~
2. ~~Выделить слой работы с данными и бизнес-логики~~
3. ~~Добавить обработку ошибок и более надёжное управление ресурсами~~
4. Добавить тестирование
5. Перейти на PostgreSQL
6. Добавить Docker
7. Реализовать REST API на FastAPI

## Статус проекта

Учебный проект, версия 0.5 — реализовано хранение данных в SQLite и разделение приложения на слои Service и Repository, а также безопасное управление курсорами и транзакциями.
